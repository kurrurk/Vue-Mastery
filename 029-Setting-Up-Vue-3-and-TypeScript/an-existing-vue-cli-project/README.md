## Converting a Vue CLI Project to TypeScript

For projects created with Vue CLI, TypeScript support can usually be added by running:

```bash
vue add typescript
```

In some cases, additional configuration may be required depending on the project's setup and dependencies.
